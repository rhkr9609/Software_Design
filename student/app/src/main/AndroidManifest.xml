<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.student.app">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/knu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/knu"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".studentActivity"></activity>
        <activity android:name=".signupActivity"></activity>
        <activity android:name=".chargeActivity"></activity>
        <activity android:name=".cornerActivity"></activity>
        <activity android:name=".order_checkActivity"></activity>
        <!-- 스플래시 액티비티 -->
        <activity android:name=".SplashActivity"
            android:theme="@style/SplashTherme"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>